{
  "name": "darou-salam-manager",
  "private": true,
  "scripts": {
    "build": "node prepare-site.cjs",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check script.js && node --check shops.js && node --check sales-orders.js && node --check invoice-pdf.js"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8",
    "jsdom": "^30.0.1"
  }
}
